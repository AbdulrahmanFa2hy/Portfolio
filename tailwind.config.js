/** @type {import('tailwindcss').Config} */
export const content = [
  "./index.html", // If your index.html is in the root
  "./src/**/*.{js,jsx,ts,tsx}", // Include all your JS/JSX/TS/TSX files inside the src folder
];
export const theme = {
  extend: {},
};
export const plugins = [];
